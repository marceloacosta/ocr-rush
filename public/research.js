import {usesS3} from './model.js';
import {SOURCES,PRICING as P,money,smallMoney,duration} from './pricing.js';
export function latencyDetails(r){
 const labels={startup:'Waiting for startup',queue:'Queue wait',layout:'Layout processing',inference:'Inference processing',retry:'Retry visibility wait'},colors={startup:'#cd762f',queue:'#b59a55',layout:'#9471b3',inference:'#397ca6',retry:'#b55259'};
 return `<section class="latency-panel" aria-labelledby="latency-title"><h3 id="latency-title">Where the time went</h3><p>${r.done}/${r.target} valid PDFs delivered · ${r.donePages.toLocaleString('en-US')} pages in the hour.</p>${r.mean===null?'<p>No valid PDFs finished. Latency statistics are unavailable; unfinished work still fails the delivery target.</p>':`<div class="latency-headline"><strong>Mean ${duration(r.mean)}</strong><span>P95 ${duration(r.p95)}</span><span>Last delivery at ${duration(r.lastFinish)} into the shift</span></div><div class="latency-bar" aria-hidden="true">${Object.entries(r.latency).map(([key,seconds])=>`<span style="flex:${seconds};background:${colors[key]}"></span>`).join('')}</div><dl class="latency-legend">${Object.entries(labels).map(([key,label])=>`<div><dt><i style="background:${colors[key]}"></i>${label}</dt><dd>${duration(r.latency[key])}</dd></div>`).join('')}</dl><p class="cost-scope">Components add to the mean arrival-to-result time for delivered valid PDFs. P95 is a separate percentile of those PDFs; ${r.target-r.done} undelivered valid PDFs are excluded from latency statistics and still count against the target. Startup means waiting while no node in that stage is ready. Retries include repeated processing in the corresponding stage.</p>`}</section>`;
}
export function costDetails(r){
 const rows=[['L40S inference',`${(r.usage.gpuSeconds/3600).toFixed(2)} running node-hours at about $3.76/hour`,r.costs.gpu],['T4 layout',`${(r.usage.layoutSeconds/3600).toFixed(2)} running node-hours at about $1.51/hour`,r.costs.layout],['Persistent platform','One hour of the approximately $530.00/month platform',r.costs.base],['GPU root disks',`${((r.usage.gpuSeconds+r.usage.layoutSeconds)/3600).toFixed(2)} node-hours with a 200 GB disk per node`,r.costs.disks],...(r.config.queue==='sqs'?[['SQS Standard',`${r.usage.sqsRequests.toLocaleString('en-US')} requests at $0.40 per million`,r.costs.sqs]]:[]),...(usesS3(r.config)?[['S3 storage and requests',`${r.usage.s3Puts.toLocaleString('en-US')} PUTs and ${r.usage.s3Gets.toLocaleString('en-US')} GET/HEADs, plus storage during this hour`,r.costs.s3]]:[])];

 return `<details class="cost-receipt" open><summary>Your modeled AWS bill · ${money(r.cost)} USD</summary><p>These estimates use a pinned Frankfurt infrastructure price snapshot for a one-hour run. Amounts are displayed in USD to two decimal places; calculations retain the full source rates. Charges below one cent are labeled explicitly. Pending nodes incur no GPU or disk charge. ${usesS3(r.config)?'The selected variation adds document storage in S3.':'Documents and results stay in Redis, which is included in the platform cost. This design has no S3 or SQS document charges.'}</p><div class="cost-table-scroll"><table><thead><tr><th>Service</th><th>Metered usage</th><th>Est. USD</th></tr></thead><tbody>${rows.map(([label,usage,cost])=>`<tr><td>${label}</td><td>${usage}</td><td>${smallMoney(cost)}</td></tr>`).join('')}</tbody><tfoot><tr><th colspan="2">Total modeled cost</th><th>${money(r.cost)}</th></tr></tfoot></table></div><p>${r.donePages?`${money(r.cost/r.donePages*1000)} per 1,000 delivered pages. `:''}Inference node time: ${duration(r.usage.busySeconds)} processing, ${duration(r.usage.bootSeconds)} startup, ${duration(r.usage.idleSeconds)} idle, summed across nodes.</p><p class="cost-scope">The $530.00/month platform snapshot includes EKS, CPU/API/system nodes, Redis, baseline NAT/NLB, EFS/ECR and non-GPU disks. All variants retain this existing platform base; decommissioning the Redis node is outside this comparison. GPU disks accrue with nodes in this model. Variable network traffic, logs, post-shift retention, tax, engineering and discounts are excluded. This is a planning estimate, not a complete production bill.</p></details>
 <details class="managed-option"><summary>What if I used Amazon Textract?</summary><p>For ${r.pages.toLocaleString('en-US')} input pages, the published Oregon example rate gives <strong>${money(r.pages*.0015)} for text detection</strong>, or <strong>${money(r.pages*.065)} for forms + tables</strong>. These are API-only estimates at the first-million-page tier, before free allowances. Your EKS workers would be replaced, not added to these figures.</p><p>These outputs are different products. Text recognition, structured forms, tables, and vision-language parsing are not interchangeable. Compare extraction quality on your own documents before comparing cost. This panel is a cost experiment, not a Textract latency simulation.</p><p>Multi-page PDF processing uses asynchronous operations. Textract has separate Start/Get request quotas and concurrent-job quotas; a higher request rate does not guarantee faster OCR. PDFs support up to 3,000 pages / 500 MB asynchronously; synchronous PDFs are limited to one page / 10 MB.</p><a href="https://aws.amazon.com/textract/pricing/" target="_blank" rel="noopener">AWS prices by feature ↗</a> · <a href="https://docs.aws.amazon.com/textract/latest/dg/limits-document.html" target="_blank" rel="noopener">Document limits ↗</a> · <a href="https://docs.aws.amazon.com/textract/latest/dg/limits-quotas-explained.html" target="_blank" rel="noopener">Request & concurrency quotas ↗</a></details>`;
}
export function modelAssumptions(costOnly=false){
 return `<h3>What the starting implementation represents</h3><p>The initial selection uses Redis for document data, task state and results. A Python layout worker removes task IDs from a Redis list, prepares pages on T4, calls Qwen3.5-4B through vLLM on L40S, and records the result or an error in Redis. Model weights are shared through EFS. The client submits and retrieves work through the API.</p><p>S3 storage, SQS retry handling, conditional output writes and a dead-letter queue are selectable variations. Moving files to S3 alone does not recover a job that has been removed from the Redis list. Redis errors remain recorded; a worker failure beyond request-level recovery can leave a job without automatic resubmission.</p>
 ${costOnly?'':`<h3>How processing time is estimated</h3><p>Performance is not a measured Qwen3.5-4B/L40S benchmark. Every PDF has a page count and an estimated output density: 80 tokens per page for receipts and 200 for other documents. An inference node has an assumed aggregate ceiling of 360 output tokens per second, with at most 40 per document stream. Concurrent streams share that finite capacity. A stream represents work across page regions; it is not a whole long PDF placed into one prompt.</p><p>The reference worker collects up to four documents over 100 ms, then processes regions concurrently through its SDK. This game simplifies that scheduler into document streams. It does not model memory pressure, image-prefill scheduling, extraction quality or page-level streaming. Layout completes for the whole PDF before inference begins. Each T4 node shares an assumed 12 pages per second across up to eight documents, or 2 pages per second for difficult scans.</p><p>The simulation resolves half-second steps and displays one frame per ten simulated seconds. Each deadline is a stated requirement of the scenario. Processing rates, startup times and budgets are exercise inputs, not AWS service guarantees.</p>
 <h3>What changes when workers start and stop</h3><p>One ready worker in each GPU pool represents an hour within the starting design’s weekday business-hours warm period. Its KEDA configuration monitors Redis queue length for layout and vLLM waiting requests for inference. Its Terraform configuration permits two inference nodes, while KEDA permits four inference pods. Those are different limits.</p><p>The queue-activated variation adds an external wake-up signal for both pools and assumes a node autoscaler or equivalent lifecycle controller. A vLLM metric cannot, by itself, wake a server that has stopped. The model assumes 90 seconds for layout startup, 240 seconds for inference startup, and 300 idle seconds before nodes are removed after work clears. Removing pods without removing nodes would keep the EC2 bill running.</p><p>The scheduled option uses a window chosen for the upload in the exercise. Nodes start at minute 6, inference is ready at minute 10, and capacity stays through minute 35 before finishing outstanding work and cooling down. Inputs arriving after shutdown require another activation window. Increasing the available-node limit assumes the node group, regional GPU quota and instance availability support the change.</p>
 <h3>How failures and completion times are counted</h3><p>The SQS variation leases jobs before layout and retains them through inference, with up to 256 held jobs per layout node. It extends a 120-second visibility lease every 30 seconds. A failed attempt stops those extensions; expiry permits a retry from layout onward. Retrying does not reset the original delivery deadline. Replacement inference capacity takes an assumed 240 seconds, billed as a continuous node interval for simplicity.</p><p>Invalid files consume an assumed 400 output-token equivalents before parsing fails. Redis records that failure without a job retry. SQS returns the job after visibility expiry; the dead-letter option moves it aside after two failed attempts. A retained error is not a successful extraction. The failure scenario deliberately models a failure beyond the worker SDK’s short request retries.</p><p>The latency chart divides a delivered PDF’s elapsed time into waiting for an unavailable stage, queue wait, layout processing, inference processing and retry visibility wait. Repeated processing belongs to its corresponding stage. The components add to mean arrival-to-result time. The 95th percentile covers delivered valid PDFs only; undelivered work still fails the scenario’s completion requirement.</p>`}
 <h3>How the USD estimate is calculated</h3><p>Prices were reviewed ${P.reviewed} and are pinned references, not a live quotation. Displayed amounts use two decimal places. The underlying calculation retains the source rates, including small request and storage charges.</p><p>L40S inference costs about $3.76 per running g6e.4xlarge hour, and T4 layout about $1.51 per running g4dn.4xlarge hour. Linux EC2 metering includes startup and idle time, with a 60-second minimum per start and whole seconds thereafter. The persistent platform is approximately $530.00 per month divided by 730 hours. It includes EKS, CPU/API/system nodes, Redis, baseline NAT/NLB, EFS/ECR and non-GPU disks, and remains charged when GPU nodes stop.</p><p>GPU root disks are assumed to be 200 GB gp3 per running node, at 9.52 cents per GB-month. The model removes those disks with their nodes; retained volumes would keep costing money. Requesting a node that remains pending adds no GPU or disk charge.</p><p>When SQS is selected, its requests cost $0.40 per million. The estimate counts sends, receives, deletes, visibility extensions, dead-letter handling and a polling allowance. When S3 is selected, Frankfurt rates are $5.40 per million PUTs, $0.43 per million GET/HEADs and 2.45 cents per GB-month. Input pages are assumed to occupy 1 MiB and output pages 0.02 MiB. Storage accrues only through the end of this hour. Redis-only processing has no S3 or SQS document charges.</p><p>Retries add their actual simulated compute and request usage. Conditional output writes have no invented fixed surcharge. Costs exclude variable network traffic, logs, storage after the run, tax, engineering and account-specific discounts. Tokens represent processing work in this model; self-hosted GPU billing is based on instance time.</p><h3>Sources for the price estimates</h3><p>These references explain the source rates and billing rules used by the calculator.</p><ul>${SOURCES.map(([label,url])=>`<li><a href="${url}" target="_blank" rel="noopener">${label} ↗</a></li>`).join('')}</ul>`;
}
